import React, { Component } from "react";
import { withRouter } from "react-router";

class AdminAccounts extends Component {
  state = {};
  render() {
    return <div>This is the admin accounts page</div>;
  }
}

export default withRouter(AdminAccounts);
