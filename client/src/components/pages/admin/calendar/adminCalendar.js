import React from "react";
import { withRouter } from "react-router";

class AdminCalendar extends Component {
  state = {};
  render() {
    return <div>This is the admin calendar page</div>;
  }
}

export default withRouter(AdminCalendar);
