import React from "react";
import { withRouter } from "react-router";

class AdminLeaves extends Component {
  state = {};
  render() {
    return <div>This is the admin leaves page</div>;
  }
}

export default withRouter(AdminLeaves);
